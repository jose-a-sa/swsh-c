sudo: required

dist: trusty

language: c

compiler:  
    - gcc
    - clang

before_script:
    - sudo apt-get -qq update && sudo apt-get install -y libgsl-dev
    - mkdir _build
    - cd _build
    - cmake ..

script: make  